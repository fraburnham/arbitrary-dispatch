from distutils.core import setup

setup(
    name='arbitrary_dispatch',
    version='0.0.1',
    description='Dispatch via an arbitrary function',
    url='https://www.github.com/fraburnham/arbitrary-dispatch'
)
